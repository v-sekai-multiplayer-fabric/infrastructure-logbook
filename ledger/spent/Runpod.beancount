;; 1-transport/runpod -- generated by misc/scripts/ledger.py, do not edit.
;; The open directive carries this project's CITATION.cff, so the book says
;; what the work was and what may be done with it without opening the repo.

2017-12-17 open Expenses:Other:Runpod  SECONDS
  abstract: "A transport layer with no listening socket. A RunPod Serverless worker is a client of its own endpoint, so this takes jobs from a queue over HTTP and hands each one to an interactor as a command. Two workers come out of "
  cff-commit: "6363097c92b2c5dc491c792a17db5601c6a41c90"
  cff-copied: "2026-08-17T18:45:22-07:00"
  cff-version: "1.2.0"
  date-released: 2026-08-17
  first-commit: 2026-08-14
  license: "MIT"
  repository-code: "https://github.com/v-sekai-multiplayer-fabric/transport-runpod"
  title: "transport-runpod: the RunPod Serverless transport layer"
  version: "0.1.0-dev.1"

2026-08-14 * "1-transport/runpod" "Split the worker into a linkable library and a thin echo main"
  spent: TRUE
  commits: 9
  steps: "A RunPod Serverless worker in C++ | Set up buildx so the GHA cache can export | Send the heartbeat before taking jobs | Authenticate every worker call, not just the ping | Write OTel NDJSON records to the network volume | Drop a duplicated status guard | Merge commit '87c232b7831c6cce78cf6455cef13a010ac9bb93' as 'third_party/ | Squashed 'third_party/interactor/' content from commit 2475326 | Split the worker into a linkable library and a thin echo main"
  Expenses:Other:Runpod                     4942 SECONDS
  Income:Sessions

2026-08-18 * "1-transport/runpod" "Publish both worker images, and let the agreement be checked without a bus"
  spent: TRUE
  commits: 4
  steps: "Merge commit 'adf76fbe63f00ce8d1da5a9ac842c3ae92872973' as 'third_party/ | Squashed 'third_party/harness/' content from commit 21d72cd | Answer a job by asking an interactor in another process | Publish both worker images, and let the agreement be checked without a b"
  Expenses:Other:Runpod                     2269 SECONDS
  Income:Sessions
