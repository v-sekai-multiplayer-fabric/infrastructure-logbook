2026-08-18 'plan: E run the pipeline once
    # uuid: 4b1f8d27-3c60-5a49-8e12-6d5f0a7c9b34
    ; 1-transport/xr-grid -- HYPOTHETICAL. Nothing here was spent.
    ; The second notch, chosen now that P0 is closed and the state is known:
    ; geogram links on macOS against the manifest revision, the library builds
    ; 853/853, and 71 of 72 tests pass.
    ;
    ; It sits in this book rather than the mesher's because the pipeline's open
    ; end is here. simple_sketch.gd already builds an ArrayMesh from a stroke and
    ; interactor-triangulation already turns a closed polygon into a mesh; what
    ; has never run is one going into the other. The seconds will be spent
    ; wherever that seam is made, and the seam is on the pen's side.
    ;
    ; Three points are the 2-4 commit row, the same row P0 and S0 used. The row is
    ; read rather than felt, because estimates made by feel here run two to four
    ; times optimistic.
    ; task: "E"
    ; depends: "P0"
    ; hypothetical: TRUE
    ; optimistic: 216
    ; likely: 2916
    ; pessimistic: 39276
    Liabilities:Planned:Mesh:Xr-Grid      8526 PLANNED-SECONDS
    Equity:Plan
