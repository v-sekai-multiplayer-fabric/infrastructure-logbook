2026-08-18 'plan: S0 let one owner hold the linked libraries so the workspace syncs
    # uuid: 27abe22b-746b-51fa-9135-f3c8ce1ae515
    ; 3-interactor/triangulation -- HYPOTHETICAL. Nothing here was spent.
    ; Accomplished. The seconds these cost are in ledger/spent/Triangulation.txn,
    ; booked from git, and never here: a plan that recorded its own completion
    ; would be reporting itself.
    ;
    ; This notch is not on the P0-E-A-B-G rail. It jumped the queue because it
    ; had to: geogram and pmp-library arrived twice, once as manifest projects
    ; and once as subtrees, and two owners of one path aborted every `repo sync`
    ; in the workspace. Nothing else could be built or measured until it went.
    ;
    ; Three points are the 2-4 commit row, which is what it took. The row is read
    ; rather than felt, because estimates made by feel here run two to four times
    ; optimistic.
    ; task: "S0"
    ; depends: ""
    ; done: 2026-08-18
    ; hypothetical: TRUE
    ; optimistic: 216
    ; likely: 2916
    ; pessimistic: 39276
    Liabilities:Planned:Mesh:Triangulation      8526 PLANNED-SECONDS
    Equity:Plan

2026-08-18 'plan: P0 link geogram on macOS
    # uuid: 9f2c1e04-7a55-5d18-9a6e-4c0b7f3ad812
    ; 3-interactor/triangulation -- HYPOTHETICAL. Nothing here was spent.
    ; The first task, and the whole of the day: 98% likely to fit inside one
    ; eight-hour day, where P0 -> E is 94.4% and the full chain 68.8%.
    ;
    ; Moved here from ledger/planned/Patch-Verify.txn, unchanged in its three
    ; points, because that book is gone. Two things were wrong with where it sat.
    ; CASSIE is deprecated -- we do not work with Unity corp -- so
    ; contract-patch-verify, the Lean oracle for CASSIE patch detection, was
    ; archived and left the manifest with the other three. And the task never
    ; belonged there anyway: geogram is linked by interactor-triangulation at
    ; libs/geogram, so the seconds were always going to be spent in this book.
    ; A task filed against a repository that cannot spend them is a plan nothing
    ; can settle against.
    ;
    ; It stays the first notch. Nothing downstream runs until the library the
    ; triangulator links builds on the machine the pen is drawn on.
    ; task: "P0"
    ; depends: ""
    ; done: 2026-08-18
    ; hypothetical: TRUE
    ; optimistic: 216
    ; likely: 2916
    ; pessimistic: 39276
    Liabilities:Planned:Mesh:Triangulation      8526 PLANNED-SECONDS
    Equity:Plan
