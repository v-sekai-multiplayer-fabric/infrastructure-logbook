;; Generated by misc/scripts/ledger.py -- do not edit.
;;
;; SPENT. Every hour below went somewhere; nothing here is an estimate. The
;; estimates are in planned.beancount, booked as liabilities, and the two share
;; no account so that a plan can never read as progress.
;;
;; Time is allocated, not summed. There is one pool -- every interval between
;; consecutive commits anywhere in the workspace -- and each interval is charged
;; to the repository whose commit closed it. That makes the charge a partition, so
;; a day totals the union of the active spans rather than the sum of overlapping
;; ones. Adding each repository's own sessions charged 2026-08-16 with 40.04 h,
;; which no day contains; allocated, the same day costs 30,312 s.
;;
;; An interval over four hours is somebody going away rather than working slowly,
;; and is charged to nobody, so a session's first commit books no time. The ledger
;; cannot see the work before it either: this is a lower bound on effort.
;;
;; Seconds are booked by author, not by repository. A fork holds upstream's commits
;; and ours, and skipping the whole checkout skipped both.

option "title" "fabric-spent: seconds booked from git sessions"
option "operating_currency" "SECONDS"

2017-12-17 open Income:Sessions  SECONDS

2026-08-17 event "deliverable" "A player draws a closed curve in VR, gets a mesh back, and the mesh is correct by a check that can fail"

include "spent/Assembly.beancount"
include "spent/Asset.beancount"
include "spent/Behaviour.beancount"
include "spent/Blender-Mcp.beancount"
include "spent/Bus-Cli.beancount"
include "spent/Cassie.beancount"
include "spent/Client.beancount"
include "spent/Connection-Fsm.beancount"
include "spent/Dot-Claude.beancount"
include "spent/Duckdb.beancount"
include "spent/Entity-Packet.beancount"
include "spent/Entity-Store.beancount"
include "spent/Fabric.beancount"
include "spent/Fanout.beancount"
include "spent/Flow-Project.beancount"
include "spent/Flow.beancount"
include "spent/Flowref-Decompiler.beancount"
include "spent/Flowref.beancount"
include "spent/Foundationdb.beancount"
include "spent/Gateway-C.beancount"
include "spent/Gateway-Python.beancount"
include "spent/Godot-Mcp.beancount"
include "spent/Godot.beancount"
include "spent/Gyre.beancount"
include "spent/Http3-Queue.beancount"
include "spent/Humanoid-Rom.beancount"
include "spent/Idtx-Flow.beancount"
include "spent/Images.beancount"
include "spent/Ingest-C.beancount"
include "spent/Ingest-Python.beancount"
include "spent/Interest-Mgmt.beancount"
include "spent/Lean-Combat.beancount"
include "spent/Lean-Loot.beancount"
include "spent/Lean-Progression.beancount"
include "spent/Lean-Rebac.beancount"
include "spent/Lean-Shared.beancount"
include "spent/Logbook.beancount"
include "spent/Manuals.beancount"
include "spent/Model-Explorer.beancount"
include "spent/Parsec.beancount"
include "spent/Patch-Verify.beancount"
include "spent/Physics.beancount"
include "spent/Protocol.beancount"
include "spent/Queen.beancount"
include "spent/Runpod.beancount"
include "spent/See-Through-Cpp.beancount"
include "spent/See-Through-Python.beancount"
include "spent/See-Through.beancount"
include "spent/Sketch.beancount"
include "spent/Spatial-Oracle.beancount"
include "spent/Taskweft.beancount"
include "spent/Triangulation.beancount"
include "spent/Ward.beancount"
include "spent/Wt.beancount"
include "spent/Xr-Grid.beancount"
